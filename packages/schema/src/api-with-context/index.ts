import { initG } from "../output";
const {
  field,
  fields,
  interface: interfaceType,
  interfaceField,
  object,
  union,
} = initG<unknown>();
export {
  field,
  fields,
  interfaceType as interface,
  interfaceField,
  object,
  union,
};
